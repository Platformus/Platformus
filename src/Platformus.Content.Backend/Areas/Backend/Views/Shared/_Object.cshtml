@* Copyright © 2015 Dmitry Sikorsky. All rights reserved. *@
@* Licensed under the Apache License, Version 2.0. See License.txt in the project root for license information. *@
@model Platformus.Content.Backend.ViewModels.Shared.ObjectViewModel
<tr class="table__row">
  @if (this.Model.Class.IsStandalone)
  {
    <td class="table__cell">@Model.Url</td>
  }
  @foreach (var property in this.Model.Properties)
  {
    <td class="table__cell">@property</td>
  }
  @foreach (var relatedClass in this.Model.RelatedClasses)
  {
    <td class="table__cell">
      <a href="/backend/objects?classid=@relatedClass.Id&objectid=@Model.Id">@relatedClass.PluralizedName</a>
    </td>
  }
  <td class="table__cell table__buttons buttons">
    <a class="table__button buttons__button buttons__button--minor button button--positive button--minor" href="@(this.Context.Request.CombineUrl("/backend/objects/edit/" + this.Model.Id))">Edit</a>
    <a class="table__button buttons__button buttons__button--minor button button--negative button--minor button--delete" href="#" onclick="return platformus.forms.deleteForm.show('/backend/objects/delete/@Model.Id');"></a>
  </td>
</tr>