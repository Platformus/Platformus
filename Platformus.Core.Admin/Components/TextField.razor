@* Copyright © 2025 Dmitry Sikorsky. All rights reserved.                                                       *@
@* Licensed under the Apache License, Version 2.0. See License.txt in the project root for license information. *@
@inherits InputText

<div class="mb-3">
  @if (!string.IsNullOrWhiteSpace(DisplayName ?? ValueExpression!.GetDisplayName()))
  {
    <label class="form-label">
      @if (Culture != null)
      {
        <span class="badge text-bg-light me-1">@Culture!.Id</span>
      }
      @(DisplayName ?? ValueExpression!.GetDisplayName())
    </label>
  }
  @if (IsMultiline)
  {
    <textarea class="@CssClass form-control" rows="@Rows" @bind="@CurrentValue" @attributes="AdditionalAttributes"></textarea>
  }
  else
  {
    <input class="@CssClass form-control" type="text" @bind="@CurrentValue" @attributes="AdditionalAttributes" />
  }
  @if (this.ValueExpression != null)
  {
    <ValidationMessage class="text-danger" For=@ValueExpression />
  }
</div>

@code {
  [Parameter] public Culture? Culture { get; set; }
  [Parameter] public bool IsMultiline { get; set; } = false;
  [Parameter] public int? Rows { get; set; }
}